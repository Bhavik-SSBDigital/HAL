model Role {
  id        Int       @id @default(autoincrement())
  branchId  Int
  status    String
  role      String
  createdAt DateTime  @default(now())
  updatedAt DateTime? @updatedAt
  branch    Department @relation(fields: [branchId], references: [id])
  users     UserRole[] // Relation to UserRole
}
